apiVersion: engine.shadow-diff.io/v1alpha1
kind: ShadowTest
metadata:
  name: otel-rmq-test-shadow
  namespace: default
spec:
  targetDeployment: rmq-prod-target

  oldImage: nodejs-test-worker:dev
  newImage: nodejs-test-worker:dev

  servicePort: 8888
  applicationPort: 8080

  inputs:
    - driver: rabbitmq_message
      amqp:
        prodUrl: amqp://rmq-prod-broker.default.svc.cluster.local:5672/
        credentialsSecretRef:
          name: rmq-prod-creds
        exchange: orders
        routingKey: order.created
        targetDependency: rabbitmq


  dependencies:
    - name: rabbitmq
      type: rabbitmq
      envVarInjection: AMQP_URL
