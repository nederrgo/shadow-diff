# Prod target for Monarch dependency-update integration tests.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: monarch-deps-update-target
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monarch-deps-update-target
  template:
    metadata:
      labels:
        app: monarch-deps-update-target
    spec:
      containers:
        - name: app
          image: nginx:alpine
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: monarch-deps-update-target
  namespace: default
spec:
  selector:
    app: monarch-deps-update-target
  ports:
    - port: 8888
      targetPort: 80
