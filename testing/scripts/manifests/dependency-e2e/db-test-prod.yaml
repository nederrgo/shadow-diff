# Production target for dependency E2E (no REDIS_HOST — Monarch injects on shadow pods only).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-test-prod
  namespace: default
  labels:
    app: db-test-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-test-prod
  template:
    metadata:
      labels:
        app: db-test-prod
    spec:
      containers:
        - name: app
          image: db-test-app:dev
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: db-test-prod
  namespace: default
spec:
  selector:
    app: db-test-prod
  ports:
    - name: http
      port: 8080
      targetPort: 8080
