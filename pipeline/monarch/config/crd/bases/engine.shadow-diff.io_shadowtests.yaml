---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: shadowtests.engine.shadow-diff.io
spec:
  group: engine.shadow-diff.io
  names:
    kind: ShadowTest
    listKind: ShadowTestList
    plural: shadowtests
    shortNames:
    - st
    singular: shadowtest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.bootStep
      name: Boot Step
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ShadowTest is the Schema for the shadowtests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ShadowTest
            properties:
              applicationPort:
                description: |-
                  ApplicationPort is the TCP port the app container listens on (Envoy forwards here).
                  When unset, Monarch derives it from the target Deployment's container ports (prefers the
                  port named "http"; falls back to the single declared port; fails if ambiguous).
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              beru:
                description: Beru overrides the beru-local analytics image (one per
                  shadow namespace).
                properties:
                  image:
                    description: Image overrides the default Beru container image.
                    type: string
                type: object
              beruGRPCTimeout:
                description: BeruGRPCTimeout is the ext_proc gRPC timeout (e.g. "2s").
                type: string
              dependencies:
                description: Dependencies lists ephemeral backing services (e.g. Redis)
                  provisioned once per shadow role.
                items:
                  description: DependencySpec declares an ephemeral backing service
                    provisioned per shadow role.
                  properties:
                    envVarInjection:
                      description: |-
                        EnvVarInjection is the app container env var name set to the role-specific dependency
                        endpoint as host:port (e.g. redis-control-a.<shadow-ns>.svc.cluster.local:6379).
                      type: string
                    image:
                      description: Image is the container image (e.g. redis:7-alpine).
                      type: string
                    name:
                      description: Name is the logical dependency id; used in resource
                        names and DNS labels.
                      type: string
                    port:
                      description: Port is the TCP port exposed by the dependency
                        container and Service.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    type:
                      description: |-
                        Type is the technology classification (e.g. rabbitmq, mongodb, redis).
                        Monarch uses this to auto-populate default images and ports.
                      type: string
                  required:
                  - envVarInjection
                  - name
                  - type
                  type: object
                type: array
              egressRelayRabbitmq:
                description: EgressRelayRabbitMQ overrides the egress-relay-rabbitmq
                  Firehose translator when inputs use rabbitmq_message.
                properties:
                  image:
                    description: Image overrides the default egress-relay-rabbitmq
                      container image.
                    type: string
                  replicas:
                    description: Replicas defaults to 1.
                    format: int32
                    type: integer
                  resources:
                    description: Resources for the egress-relay-rabbitmq container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              igris:
                description: Igris overrides image, replicas, or resources for the
                  Igris traffic hub (HTTP/TCP ingress).
                properties:
                  image:
                    description: Image overrides the default Igris container image.
                    type: string
                  replicas:
                    description: Replicas defaults to 1.
                    format: int32
                    type: integer
                  resources:
                    description: Resources for the Igris container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              igrisRabbitmq:
                description: IgrisRabbitMQ overrides the igris-rabbitmq workload when
                  inputs use rabbitmq_message.
                properties:
                  image:
                    description: Image overrides the default igris-rabbitmq container
                      image.
                    type: string
                  replicas:
                    description: Replicas defaults to 1.
                    format: int32
                    type: integer
                  resources:
                    description: Resources for the igris-rabbitmq container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              inputs:
                description: |-
                  Inputs defines Igris listener ports and drivers. When empty, Monarch defaults to
                  a single HTTP listener on servicePort.
                items:
                  description: 'InputSpec declares an ingress driver: HTTP listeners
                    or RabbitMQ message capture.'
                  properties:
                    addon:
                      description: Addon is deprecated; use driver. Legacy value "http"
                        maps to http_request.
                      type: string
                    amqp:
                      description: AMQP holds broker settings when driver is rabbitmq_message.
                      properties:
                        exchange:
                          description: Exchange is the production exchange to bind
                            the shadow queue to.
                          type: string
                        exchangeType:
                          description: ExchangeType is the AMQP exchange type (topic,
                            direct, fanout, headers). Defaults to topic.
                          enum:
                          - topic
                          - direct
                          - fanout
                          - headers
                          type: string
                        prodUrl:
                          description: ProdURL is the production broker URL (e.g.
                            amqp://prod-rabbitmq.default.svc:5672).
                          type: string
                        routingKey:
                          description: RoutingKey is the binding routing key (e.g.
                            "#" or "orders.*").
                          type: string
                        targetDependency:
                          description: TargetDependency is the name of a spec.dependencies
                            entry (shadow brokers per role).
                          type: string
                      required:
                      - exchange
                      - prodUrl
                      - routingKey
                      - targetDependency
                      type: object
                    driver:
                      description: |-
                        Driver selects the ingress path (http_request or rabbitmq_message).
                        When empty on a port-based input, Monarch defaults to http_request.
                      enum:
                      - http_request
                      - rabbitmq_message
                      type: string
                    port:
                      description: Port is the TCP port Igris binds for HTTP inputs.
                        Omit for rabbitmq_message.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                  type: object
                type: array
              maxQPSPerPod:
                description: |-
                  MaxQPSPerPod caps requests/sec Igris will forward per shadow pod replica (default 50).
                  Monarch multiplies this by the shadow role replica count to compute IGRIS_MAX_CONCURRENCY,
                  the ingress load-shedding threshold that protects shadow pods from traffic spikes.
                minimum: 1
                type: integer
              mode:
                default: record
                description: Mode selects record (capture to S3) or replay (ABC +
                  S3 readers). Defaults to record.
                enum:
                - record
                - replay
                type: string
              newImage:
                description: NewImage is the container image for the Candidate pod.
                type: string
              oldImage:
                description: |-
                  OldImage is the container image for Control-A and Control-B pods.
                  When unset, Monarch derives it from the target Deployment's current running image.
                type: string
              samplePercentage:
                description: |-
                  SamplePercentage is the shared prod sampling gate (1-100, default 100) for
                  all input types. Uses github.com/shadow-diff/sample: V = FNV-1a-64(decoded
                  16-byte W3C trace id) & 0xFF; keep iff (V*100)<(N*256). Empty/missing
                  traceparent is always dropped. Monarch seeds it by inputs[].driver: HTTP →
                  KaiselRule (ingress and egress); rabbitmq_message → igris-rabbitmq
                  (IGRIS_RMQ_SAMPLE_PERCENTAGE). RabbitMQ does not use Kaisel.
                maximum: 100
                minimum: 1
                type: integer
              servicePort:
                description: |-
                  ServicePort is the TCP port the Envoy ingress listener binds on in shadow pods.
                  Monarch always computes a conflict-free value relative to applicationPort — leave unset
                  unless you have a specific networking reason to pin it.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              sessionID:
                description: |-
                  SessionID pins the S3 session folder for replay (and optionally for record).
                  When unset in record mode, Monarch mints status.currentSessionID (and remints
                  on each replay→record transition so capture does not append into the prior folder).
                type: string
              shadowSoldier:
                description: ShadowSoldier overrides the database egress capture sidecar
                  image.
                properties:
                  image:
                    description: Image overrides the default shadow-soldier container
                      image.
                    type: string
                type: object
              shop:
                description: Shop overrides the Shop mock-store image (always provisioned
                  per shadow namespace).
                properties:
                  image:
                    description: Image overrides the default Shop container image.
                    type: string
                type: object
              storage:
                description: Storage configures required BYOB S3-compatible object
                  storage for this ShadowTest.
                properties:
                  bucketName:
                    description: BucketName is the S3 bucket that holds recorded traffic
                      for this ShadowTest.
                    type: string
                  credentialsSecretRef:
                    description: |-
                      CredentialsSecretRef names a Secret in the ShadowTest CR namespace with
                      AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY. Monarch copies it into the
                      shadow namespace (same name) before injecting secretKeyRef into pods.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  endpoint:
                    description: Endpoint is an optional custom S3 API URL (MinIO
                      / on-prem).
                    type: string
                  region:
                    description: Region is the AWS region (also used as a MinIO region
                      label).
                    type: string
                  retentionPolicy:
                    default: Retain
                    description: RetentionPolicy controls whether Monarch deletes
                      the ShadowTest's S3 prefix on CR deletion.
                    enum:
                    - Retain
                    - Delete
                    type: string
                  type:
                    default: s3
                    description: Type selects the object-storage backend.
                    enum:
                    - s3
                    type: string
                required:
                - bucketName
                - type
                type: object
              targetDeployment:
                description: |-
                  TargetDeployment is the name of the production Deployment whose pod template
                  (inline env vars from the first container) is mirrored for shadow pods.
                type: string
              targetNamespace:
                description: |-
                  TargetNamespace is the namespace containing TargetDeployment.
                  Defaults to the ShadowTest CR namespace when unset.
                type: string
            required:
            - newImage
            - storage
            - targetDeployment
            type: object
          status:
            description: status defines the observed state of ShadowTest
            properties:
              amqpQueueName:
                description: AmqpQueueName is the production broker queue Monarch
                  declared (shadow-diff-<uid>).
                type: string
              bootStep:
                description: BootStep is the current position in the Monarch boot
                  sequence.
                type: string
              captureTargets:
                description: CaptureTargets lists discovered prod pod template labels
                  (key=value, sorted).
                items:
                  type: string
                type: array
              components:
                description: Components reports per-component readiness for the topology
                  graph.
                properties:
                  amqpBound:
                    description: |-
                      AMQPBound is true once the prod shadow queue is bound, or when the ShadowTest
                      declares no AMQP ingress at all.
                    type: boolean
                  beruReady:
                    description: BeruReady is true when the beru-local analysis sink
                      is Available.
                    type: boolean
                  igrisReady:
                    description: IgrisReady is true when the ingress hub (igris or
                      igris-rabbitmq) is Available.
                    type: boolean
                  ingressDrivers:
                    description: |-
                      IngressDrivers lists resolved spec.inputs[].driver values
                      (e.g. http_request, rabbitmq_message). Tusk uses this to gate topology nodes.
                    items:
                      type: string
                    type: array
                  kaiselRuleActive:
                    description: KaiselRuleActive is true when the eBPF capture rule
                      reconciled cleanly (record mode).
                    type: boolean
                  shadowRolesReady:
                    additionalProperties:
                      type: boolean
                    description: |-
                      ShadowRolesReady maps control-a/control-b/candidate to Deployment readiness.
                      Empty in record mode, where no shadow roles are provisioned.
                    type: object
                  shopReady:
                    description: ShopReady is true when the per-ShadowTest egress
                      mock store is Available.
                    type: boolean
                  targetDeployment:
                    description: TargetDeployment is the production Deployment this
                      ShadowTest shadows.
                    type: string
                type: object
              conditions:
                description: |-
                  Conditions follow the standard Kubernetes condition contract.
                  Types: Ready, Progressing, Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentReplayExecutionID:
                description: |-
                  CurrentReplayExecutionID scopes Postgres diffs for one replay run of CurrentSessionID.
                  Minted when entering replay; injected as REPLAY_EXECUTION_ID on beru-local.
                type: string
              currentSessionID:
                description: CurrentSessionID is the active S3 session folder for
                  this ShadowTest.
                type: string
              igrisEndpoint:
                description: IgrisEndpoint is the DNS host:port Monarch configured
                  for capture forwarding.
                type: string
              igrisRabbitMQPhase:
                description: IgrisRabbitMQPhase summarizes igris-rabbitmq deployment
                  readiness.
                type: string
              kaiselPhase:
                description: KaiselPhase summarizes Kaisel ingress capture reconciliation
                  (Ready, Degraded).
                type: string
              message:
                description: Message carries human-readable detail, including MVP
                  limitations (skipped envFrom, etc.).
                type: string
              phase:
                description: Phase is a high-level summary of reconciliation (e.g.
                  Ready, Progressing, Failed).
                type: string
              replayState:
                description: ReplayState tracks automated replay trigger progress
                  ("" | started | completed).
                type: string
              shadowNamespace:
                description: ShadowNamespace is the dedicated namespace where control/candidate
                  Deployments run.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
