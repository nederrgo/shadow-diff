apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tusk is a cluster-wide singleton BFF, not a per-ShadowTest workload, so it ships
# its own manifests rather than being provisioned by the Monarch reconciler.
resources:
  - deployment.yaml
  - service.yaml
