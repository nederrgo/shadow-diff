apiVersion: v1
kind: Service
metadata:
  name: the-system
  namespace: monarch-system
  labels:
    app.kubernetes.io/name: the-system
    app.kubernetes.io/part-of: shadow-diff
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: the-system
