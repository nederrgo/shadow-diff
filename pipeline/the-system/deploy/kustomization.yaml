apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The System is a cluster-wide singleton UI, not a per-ShadowTest workload, so it
# ships its own manifests rather than being provisioned by the Monarch reconciler.
resources:
  - deployment.yaml
  - service.yaml
